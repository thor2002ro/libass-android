diff --git a/libass/ass_drawing.c b/libass/ass_drawing.c
index b4c3233..2079808 100644
--- a/libass/ass_drawing.c
+++ b/libass/ass_drawing.c
@@ -29,6 +29,9 @@
 
 #define DRAWING_INITIAL_POINTS 100
 #define DRAWING_INITIAL_SEGMENTS 100
+#define DRAWING_TOKEN_MEMORY_LIMIT (8 * 1024 * 1024)
+#define DRAWING_TOKEN_LIMIT \
+    (DRAWING_TOKEN_MEMORY_LIMIT / sizeof(ASS_DrawingToken))
 
 /*
  * \brief Free a list of tokens
@@ -42,10 +45,13 @@ static void drawing_free_tokens(ASS_DrawingToken *token)
     }
 }
 
-static inline bool add_node(ASS_DrawingToken **tail, ASS_TokenType type, ASS_Vector point)
+static inline bool add_node(ASS_DrawingToken **tail, size_t *token_count,
+                            ASS_TokenType type, ASS_Vector point)
 {
     assert(tail && *tail);
 
+    if (*token_count >= DRAWING_TOKEN_LIMIT)
+        return false;
     ASS_DrawingToken *new_tail = malloc(sizeof(**tail));
     if (!new_tail)
         return false;
@@ -55,6 +61,7 @@ static inline bool add_node(ASS_DrawingToken **tail, ASS_TokenType type, ASS_Vec
     new_tail->type = type;
     new_tail->point = point;
     *tail = new_tail;
+    (*token_count)++;
     return true;
 }
 
@@ -72,10 +79,12 @@ static inline bool get_point(const char **str, ASS_Vector *point)
  * Parses and advances the string for exactly 3 points.
  * If the string contains fewer than 3 points,
  * any initial matching coordinates are still consumed.
- * If an allocation fails, error will be set to true.
+ * If an allocation fails or the token limit is reached,
+ * error will be set to true.
  * \return whether three valid points were added
  */
-static bool add_3_points(const char **str, ASS_DrawingToken **tail, ASS_TokenType type, bool *error)
+static bool add_3_points(const char **str, ASS_DrawingToken **tail,
+                         size_t *token_count, ASS_TokenType type, bool *error)
 {
     ASS_Vector buf[3];
 
@@ -89,9 +98,9 @@ static bool add_3_points(const char **str, ASS_DrawingToken **tail, ASS_TokenTyp
     if (!valid)
         return false;
 
-    valid = add_node(tail, type, buf[0]);
-    valid = valid && add_node(tail, type, buf[1]);
-    valid = valid && add_node(tail, type, buf[2]);
+    valid = add_node(tail, token_count, type, buf[0]);
+    valid = valid && add_node(tail, token_count, type, buf[1]);
+    valid = valid && add_node(tail, token_count, type, buf[2]);
     if (!valid) {
         *error = true;
         return false;
@@ -104,51 +113,50 @@ static bool add_3_points(const char **str, ASS_DrawingToken **tail, ASS_TokenTyp
  * Parses and advances the string while it matches points.
  * Each set of batch_size points will be turned into tokens and appended to tail.
  * Partial matches (i.e. an insufficient amount of coordinates) are still consumed.
- * If an allocation fails, error will be set to true.
- * \return count of added points
+ * If an allocation fails or the token limit is reached,
+ * error will be set to true.
  */
-static size_t add_many_points(const char **str, ASS_DrawingToken **tail,
-                              ASS_TokenType type, size_t batch_size, bool *error)
+static void add_many_points(const char **str, ASS_DrawingToken **tail,
+                            size_t *token_count, ASS_TokenType type,
+                            size_t batch_size, bool *error)
 {
     ASS_Vector buf[3];
     assert(batch_size <= (sizeof(buf) / sizeof(*buf)));
 
     if (!*str)
-        return 0;
+        return;
 
-    size_t count_total = 0;
     size_t count_batch = 0;
     while (**str) {
         ASS_Vector point;
         if (!get_point(str, &point))
             break;
         buf[count_batch] = point;
-        count_total++;
         count_batch++;
 
         if (count_batch != batch_size)
             continue;
 
         for (size_t i = 0; i < count_batch; i++)
-            if (!add_node(tail, type, buf[i])) {
+            if (!add_node(tail, token_count, type, buf[i])) {
                 *error = true;
-                return count_total - count_batch + i;
+                return;
             }
         count_batch = 0;
     }
-
-    return count_total - count_batch;
 }
 
 static inline bool add_root_node(ASS_DrawingToken **root, ASS_DrawingToken **tail,
-                                 size_t *points, ASS_Vector point, ASS_TokenType type)
+                                 size_t *token_count, ASS_Vector point, ASS_TokenType type)
 {
+    if (*token_count >= DRAWING_TOKEN_LIMIT)
+        return false;
     *root = *tail = calloc(1, sizeof(ASS_DrawingToken));
     if (!*root)
         return false;
     (*root)->point = point;
     (*root)->type = type;
-    *points = 1;
+    (*token_count)++;
     return true;
 }
 
@@ -160,7 +168,7 @@ static ASS_DrawingToken *drawing_tokenize(const char *str)
 {
     const char *p = str;
     ASS_DrawingToken *root = NULL, *tail = NULL, *spline_start = NULL;
-    size_t points = 0;
+    size_t token_count = 0;
     bool m_seen = false;
     bool error = false;
 
@@ -185,10 +193,10 @@ static ASS_DrawingToken *drawing_tokenize(const char *str)
                 ASS_Vector point;
                 if (!get_point(&p, &point))
                     continue;
-                if (!add_root_node(&root, &tail, &points, point, TOKEN_MOVE))
+                if (!add_root_node(&root, &tail, &token_count, point, TOKEN_MOVE))
                     return NULL;
             }
-            points += add_many_points(&p, &tail, TOKEN_MOVE, 1, &error);
+            add_many_points(&p, &tail, &token_count, TOKEN_MOVE, 1, &error);
             break;
         case 'n':
             if (!root) {
@@ -197,20 +205,20 @@ static ASS_DrawingToken *drawing_tokenize(const char *str)
                     continue;
                 if (!m_seen)
                     return NULL;
-                if (!add_root_node(&root, &tail, &points, point, TOKEN_MOVE_NC))
+                if (!add_root_node(&root, &tail, &token_count, point, TOKEN_MOVE_NC))
                     return NULL;
             }
-            points += add_many_points(&p, &tail, TOKEN_MOVE_NC, 1, &error);
+            add_many_points(&p, &tail, &token_count, TOKEN_MOVE_NC, 1, &error);
             break;
         case 'l':
             if (!root)
                 continue;
-            points += add_many_points(&p, &tail, TOKEN_LINE, 1, &error);
+            add_many_points(&p, &tail, &token_count, TOKEN_LINE, 1, &error);
             break;
         case 'b':
             if (!root)
                 continue;
-            points += add_many_points(&p, &tail, TOKEN_CUBIC_BEZIER, 3, &error);
+            add_many_points(&p, &tail, &token_count, TOKEN_CUBIC_BEZIER, 3, &error);
             break;
         case 's':
             if (!root)
@@ -218,23 +226,22 @@ static ASS_DrawingToken *drawing_tokenize(const char *str)
             // Only the initial 3 points are TOKEN_B_SPLINE,
             // all following ones are TOKEN_EXTEND_SPLINE
             spline_start = tail;
-            if (!add_3_points(&p, &tail, TOKEN_B_SPLINE, &error)) {
+            if (!add_3_points(&p, &tail, &token_count, TOKEN_B_SPLINE, &error)) {
                 spline_start = NULL;
                 break;
             }
-            points += 3;
             //-fallthrough
         case 'p':
-            if (points < 3)
+            if (token_count < 3)
                 continue;
-            points += add_many_points(&p, &tail, TOKEN_EXTEND_SPLINE, 1, &error);
+            add_many_points(&p, &tail, &token_count, TOKEN_EXTEND_SPLINE, 1, &error);
             break;
         case 'c':
             if (!spline_start)
                 continue;
             // Close b-splines: add the first three points of the b-spline back to the end
             for (int i = 0; i < 3; i++) {
-                if (!add_node(&tail, TOKEN_EXTEND_SPLINE, spline_start->point)) {
+                if (!add_node(&tail, &token_count, TOKEN_EXTEND_SPLINE, spline_start->point)) {
                     error = true;
                     break;
                 }
